//let usuario; //declarando variable
usuario = prompt("Ingrese Usuario");
clave = prompt("Ingrese Clave");

if (usuario == "admin" && clave == "1234")
        alert("Ingreso Correcto");        
else    
    if (usuario != "admin")
          alert("Usuario no Valido");        
    else 
            if (clave != "1234")
                alert("Contraseña Incorrecta");                            
                
    
    


